package Account;

import java.util.ArrayList;
import java.util.List;
import java.util.Map;
//
//public class UserServicceImplementation implements UserServise<Account, User>
//{
//    private Map<User, Account> account;
//
//    public UserServicceImplementation(Map<User, Account> account){
//        this.account = account;
//    }
////
////    @Override
////    public List<User> getAllUsers() {
//////        Set<User> users = account.keySet();
//////        return new ArrayList<>(users);
////    }
////
////    @Override
////    public List<User> getAllAccounts() {
////        return null;
////    }
////
////    @Override
////    public double tax(User user) {
////        for (Map.Entry<User, Account> entry : account.entrySet()){
////            if (entry.getKey().getSomeName().equals(user.getSomeName()))
////                && entry.getKey().getName().equals(user.getName()){
////                if (entry.getValue().getStatus().equals("active"))
////                return entry.getValue().getMoney() * 0.05;
////            }
////        }
////        return 0;
////    }
//}
